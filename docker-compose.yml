services:
  md2wechat:
    build: .
    ports:
      - "${HOST_PORT:-3000}:3000"
    env_file:
      - .env
    volumes:
      - ./config:/app/config
      - ./themes:/app/themes
      - ./data:/app/data
    restart: unless-stopped
